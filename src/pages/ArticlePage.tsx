import { useNavigate } from "react-router-dom";
import NavBar from "../components/NavBar";

export default function ArticlePage(){
    const router = useNavigate();

    return<>
        
        <h1>Article page placeholder</h1>
    </>
}